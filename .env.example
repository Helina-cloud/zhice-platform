# 复制本文件为 .env 后填写。切勿将含真实密钥的 .env 提交到版本库。

# ---------- DeepSeek（OpenAI 兼容）示例 ----------
# 密钥二选一：OPENAI_API_KEY 或 DEEPSEEK_API_KEY
OPENAI_API_KEY=
# DeepSeek 官方兼容地址（不要带 /v1 结尾也可，LangChain 会按兼容客户端拼接）
OPENAI_API_BASE=https://api.deepseek.com
CHAT_MODEL=deepseek-chat
# 新版模型示例：deepseek-v4-flash、deepseek-v4-pro（见官方文档）

# ---------- 仅使用 OpenAI 时 ----------
# OPENAI_API_BASE=https://api.openai.com/v1
# CHAT_MODEL=gpt-4o-mini

# ---------- 嵌入（RAG 建索引必需；DeepSeek 对话 URL 不能当 embeddings 用，否则会 404）----------
# 方式 A：嵌入走 OpenAI（对话仍可用 DeepSeek）
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_API_BASE=https://api.openai.com/v1
# 对话密钥若是 DeepSeek，此处必须单独填 OpenAI 的 sk-…（不要用 DeepSeek key）
EMBEDDING_API_KEY=

# 若「对话 + 嵌入」全是 OpenAI，可不设 EMBEDDING_API_*，会共用 OPENAI_API_KEY / OPENAI_API_BASE。

# 方式 B：本地向量（无 OpenAI 嵌入额度 / 不想为 embeddings 付费时推荐）
# EMBEDDING_PROVIDER=huggingface
# HUGGINGFACE_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# 需安装: pip install sentence-transformers

# Agent 联网搜索：pip install ddgs（推荐）；若无 ddgs 会尝试 duckduckgo-search

# 可选：覆盖默认路径（文档目录与 FAISS 向量库目录）
# DATA_DOCS_DIR=e:\code\zhice-platform\data\docs
# VECTOR_STORE_DIR=e:\code\zhice-platform\data\vector_db

CHUNK_SIZE=800
CHUNK_OVERLAP=120
RETRIEVER_K=4

# ---------- HTTPS / 代理（嵌入与对话请求共用 httpx；遇 SSL EOF / ConnectError 时再改）----------
# OPENAI_HTTP_TRUST_ENV=false
# OPENAI_HTTP_VERIFY_SSL=false
# OPENAI_PROXY=http://127.0.0.1:7890

# 增强版：业务指标 API 根地址（示例：http://127.0.0.1:8080）
# ANALYTICS_API_BASE=
# ANALYTICS_TIMEOUT_S=15
